今天原本是休息的，但是继续推进进度，明天周一有空复习复习
哈希表

数组（列表）

    适用场景：当需要存储有序的数据集合，并且需要频繁地进行索引访问、切片操作、追加或遍历时。

    优点：
        有序：保持元素的插入顺序。
        索引访问：可以通过索引快速访问任意位置的元素。
        切片操作：可以轻松获取子数组。

    缺点：
        查找某个元素的时间复杂度为 O(n)。
        在中间插入或删除元素可能需要移动其他元素，时间复杂度为 O(n)。

集合（set）

    适用场景：当需要存储唯一元素，并且需要快速查找、插入和删除操作时。

    优点：
        无序且唯一：自动去重，不保持元素的插入顺序。
        快速查找、插入和删除操作，时间复杂度为 O(1)。

    缺点：
        无法通过索引访问元素。
        无法存储重复的元素。

映射（字典）

    适用场景：当需要存储键值对，并且需要通过键快速查找、插入和删除时。

    优点：
        快速查找、插入和删除操作，时间复杂度为 O(1)。
        键值对存储，便于通过键访问对应的值。

    缺点：
        无序（Python 3.7 及之后版本的字典保持插入顺序，但这不影响性能特性）。
        键必须是可哈希的对象。


242.有效的字母异位词
数组 用来做哈希表



349. 两个数组的交集
set 用来做哈希表

第202题. 快乐数
同样是用set来解决哈希问题的一个应用
如何获得一个整数各个位置上的数？n, r = divmod(n, 10)
divmod(a, b) 函数会同时返回两个值：a 除以 b 的商和余数。


1. 两数之和
map用来做哈希表
