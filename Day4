24. Swap Nodes in Pairs
两两交换， 假设说有一个链表 1->2->3->4->5,加上dummy_head之后本质上就是dummy_head->2->1->3,以此类推。


19.删除链表的倒数第N个节点  
根据链表的删除操作，我们需要让指针指向要删除节点的前一个节点
思路：定义两个指针，一个快指针一个满指针，先让快指针走N+1步（由于要指向前一个节点），满指针初始为0。后快快慢指针同时移动直到快指针指向None
（这个想法真有点意思

